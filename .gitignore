venv/
.venv/
__pycache__/
*.pyc
build/
dist/
*.db
*.sqlite
*.sqlite3
.license_cache
.env
.env.local
.vercel
.env*
# Allow committed config templates (placeholders only — no secrets)
!.env.example
!**/.env.example
dev_config.json
hwid-client/target/
archive/rust_crypto/target/
archive/hw_client/target/
archive/barcode_gen/target/
Cargo.lock
.idea/
.next/
node_modules/
*.tsbuildinfo
labels/
push_via_api.py

# ── Refactor: FastAPI backend ──
backend_fastapi/.env
backend_fastapi/__pycache__/
backend_fastapi/tests/.pytest_cache/
backend_fastapi/*.sqlite
backend_fastapi/*.sqlite-journal
backend_fastapi/*.sqlite-wal
backend_fastapi/.coverage
*.db-shm
*.db-wal
.pharmacy_device_id

# Tauri updater signing key — NEVER commit (compromise = signed malware)
src-tauri/updater-key/

# Tauri alternative build dir (do not track build artifacts)
tauri-build-target/
