# PROJECT_MAP

## [TECH_STACK]
- UI: CustomTkinter, Tkinter (Treeview)
- DB: SQLite3, SQLAlchemy ORM
- File Handling: openpyxl
- OS Integration: subprocess, sys, os, platform
- Crypto: cryptography, pycryptodome, rust_crypto (PyO3 .pyd)
- HWID: uuid, platform, mac_address, hw_client (PyO3 .pyd)
- Other: hashlib, json, requests, pyo3 (Rust→Python bindings)

## [SYSTEM_FLOW]
- **Entry Point:** `main.py` -> `main_app.py`
- **License Gate:** `license_gate.py` intercepts launch, verifies auth.
- **Main App:** `ui.py` sets up `PharmacyApp` and initializes tabs.
- **Tabs:** Dedicated modules (`ui_dashboard_tab.py`, `ui_checkout_tab.py`, etc.) handle GUI.
- **Database:** `database.py` manages all DB ops (Products, Receipts, Patients).
- **Extracted Engines:**
  - `pos_engine.py`: Encapsulates checkout/cart state.
  - `receipt_engine.py`: Generates receipts (PDF/TXT).
  - `backup.py`: DB snapshots.
  - `audit_log.py`: Logs actions.

## [ARCHITECTURE]
- Monolithic desktop app with isolated logic engines.
- Shared state is minimized; UI queries DB or Engines.
- CustomTkinter is used for modern aesthetics. Grid geometry management is preferred.

## [ORPHANS & PENDING]
- [COMPLETE] 2026-08-06 Runtime Bug Fixes — (1) Patients insurance columns migration: added idempotent `PRAGMA table_info` + `ALTER TABLE` for `insurance_provider`, `policy_number`, `group_number` in `database.py:init_db()`, `db.py:init_db()` (ORM model + SQLite path), `rx_database.py:init_rx_tables()`, `rx_db.py:init_rx_tables()`. COALESCE fallbacks added to `ui_pos_retail.py` + `ui_pos_panels.py` patient queries. (2) TabViewCompat `_tab_dict` property added in `ui_navigation.py` delegating to `self.frames`. (3) AsyncUI safe shutdown: `winfo_exists()` guard + `except (tk.TclError, RuntimeError)` silent discard in `async_ui.py`. All 92 existing tests pass.
- [PENDING] Update `license_gate.py` to remove `DEV-PASS` and use Ghost Token/Env Var.
- [COMPLETE] Phase 8: Internationalization (i18n) — Added 58 new i18n keys to `locales/en.json` and `locales/ar.json` covering OCR cascade tier names, status badges, feedback badges, email report settings, PostgreSQL sync settings, checkout tab labels, navigation drawer, dashboard KPI cards, and dynamic format strings. Refactored `design_system.py` (CascadeStatusBadge, OCRProgressBar, OCRFeedbackBadge), `ui_navigation.py` (NavigationDrawer), `ui_dashboard_tab.py`, `ui_checkout_tab.py`, `ui_settings_tab.py`, and `local_daily_report.py` to retrieve all strings via `i18n.t()`. Dynamic formatting verified for `{tier}`, `{percent}`, `{count}`, `{total}`, `{review}`, `{message}`, `{backend}`, `{error}` placeholders. **COMPLETE: M85**
- [COMPLETE] Phase 9: Final System Validation — `test_phase9_final_validation.py` (24 tests, all PASS) covering i18n switching, async cleanup, crypto round-trip, cross-module integration. AsyncUI gained `reset()` classmethod + executor cleanup. **COMPLETE: M86**
- [COMPLETE] Phase 10: Executable Build & Deployment — Updated `PharmacyPro_Enterprise.spec` (binaries for .pyd, COLLECT onedir, added design_system/ocr_cascade/ocr_engine/hw_client hiddenimports). Updated `build_exe.py` (--onedir, --add-binary for .pyd, missing hidden imports). Added test 10.2 in `exhaustive_verify.py` for bundled .pyd + locale verification. Build: 10.5 MB standalone exe at `dist/PharmacyPro_Enterprise/PharmacyPro_Enterprise.exe`. Runtime smoke test: clean startup, no crashes/DLL errors. **FINAL: 105/105 PASS — COMPLETE: M87**
- [PENDING] Update `license_gate.py` to remove `DEV-PASS` and use Ghost Token/Env Var.
- [COMPLETE] Startup crash fixes — Resolved `TypeError: CTkFrame.__init__() got multiple values for keyword argument 'fg_color'` in 4 classes (`StatusDashboardFrame`, `EnterprisePosFrame`, `ClinicalWorkflowFrame`, `InventoryManagementFrame`) by using `kwargs.pop("fg_color", "transparent")`. Fixed `TabViewCompat` parent issue in 4 setup functions (`setup_pos_retail_tab`, `setup_clinical_workflow_tab`, `setup_quick_sig_tab`, `setup_inventory_management_tab`) — changed from `parent = self.tab_view` + `parent.add(frame, text=...)` to `parent = self.tab_*` + `frame.pack(...)`, matching the pattern used by the 6 working setup functions. Added `CTkSpinbox` compatibility shim in `ui_pos_retail.py` (CTk 6.0 removed `CTkSpinbox`). Fixed `CTkTabview.add()` API change in `ui_clinical_workflow.py` (CTk 6.0 removed `text=` kwarg). Added missing `import tkinter as tk` in `ui_clinical_workflow.py`. App starts and runs successfully; 93 existing tests pass.
- [COMPLETE] Phase 12: EPCS Workflow Module — `ui_epcs_workflow.py` created (`EpcsWorkflowFrame` with 3-step wizard: patient selection, medication selection, prescription details + authorization). Wired into `main_app.py:_wire_rx_extensions()` with nav icon `"📝"`, tab creation, and on_tab_change hook. Full EPCS submit flow: `strategy_factory(region).authenticate() → validate_prescription() → calculate_patient_cost() → generate_claim() → rx_db.add_rx_regional() → update_rx_status("Billed")`. Veterinarian/prescriber selection handles NPI-null (NPI → DEA → State License fallback). Region-aware field labels via `rx_db.get_prescriber_labels` (GB/DE mapped to EU). sqlite3 fallback for all rx_db calls. Added 28 new i18n keys to all 6 locale files. Backend files locked — not modified. 25 new tests in `test_epcs_workflow.py` (all PASS). 99 total tests pass (74 existing + 25 new) — zero regression. **COMPLETE: M89**
- [COMPLETE] Phase 11: Rx Processing Module — `ui_rx_processing.py` created (`RxProcessingFrame`, module-level helpers, `setup_rx_processing_tab`, `_refresh_rx_processing_tab`). Wired into `main_app.py:_wire_rx_extensions()` with nav icon, tab creation, and on_tab_change hook. Uses `strategy_factory()` for billing, `rx_db` for data with sqlite3 fallback, `AsyncUI` for non-blocking search, `CTkTabview` with `command` callback for queue switching. Added 35 i18n keys to all 6 locale files. **COMPLETE: M88**
- [COMPLETE] Phase 16 Chunk 3a: Status Dashboard Module — `ui_status_dashboard.py` rewritten with 8 BestRx-style metric cards (Ready for Pickup, Waiting, In Processing, Refill Requests, Third Party Ready, Third Party Reject, Insurance Reject, Waiting to be Done), 3×3 task panel, and CTkTabview queue with Treeview per tab (In Processing / Rejects / Ready for Pickup). Data sources: `rx_db.get_rx_status_counts()` + custom sqlite3 with `json_extract` on `regional_metadata`. Async loading via `AsyncUI`. Layout uses `grid_propagate(False)` on metric cards and `CTkScrollableFrame` for elastic metrics. Added 17 new i18n keys (8 metrics + 9 tasks) to all 6 locale files. 25 existing tests pass. **COMPLETE**
- [PENDING] Establish Smart, Modern UI/UX Design System in `ui.py`.
- [PENDING] Extract `pos_engine.py` from `ui_checkout_tab.py`.
- [PENDING] Extract `receipt_engine.py`.
- [PENDING] Implement `backup.py`.
- [PENDING] Implement `audit_log.py`.
- [PENDING] Update 12 core capabilities.

- [COMPLETE] Phase: Supplier Order Management — Chunk 1 (infra, `archive/ui_supplier_order_management.py` PART 1): `database.py` DDL for `suppliers`/`purchase_orders`/`po_items` + `tax_id` column + backfill; PO CRUD + `receive_po_items`/`receive_inventory_atomically`/`get_products_below_reorder_threshold`; `db.py` SQLAlchemy ORM (Supplier/PurchaseOrder/PoItem) + `tax_id` + receive funcs in `_not_available` (sqlite fallback) + `@_db_fallback` delegation; `SupplierObserver` + `SupplierCrudManager` + `PoCrudManager` (state-machine `Draft→Submitted→Received→Closed`; `_receive_with_data` §6 flow honoring per-item `receipt_data`; `auto_reorder`/`compute_low_stock_groups`). i18n keys added to all 6 locales. Verified: `py_compile` clean; temp-DB functional 23/23; `test_phase16` 25/25; `test_phase9` 24/24; `exhaustive_verify` 105/105. **COMPLETE: M90**
- [COMPLETE] Phase: Supplier Order Management — Chunk 2 (data-entry dialogs, `archive/ui_supplier_order_management.py` PART 2): `SupplierDialog` (create/update via `SupplierCrudManager.create/update`, `tax_id` field), `PoItemDialog` (add/edit line item, dynamic Line Total, Lookup stub), `ReceivePoDialog` (Submitted→Received wizard; gathers `receipt_data`, calls `PoCrudManager.transition(po_id,"Received",receipt_data=...)`). Manager support verified functionally; `py_compile` clean. **COMPLETE: M91**
- [COMPLETE] Phase: Supplier Order Management — Chunk 3 (`archive/ui_supplier_order_management.py` PART 3): `SupplierOrderManagementFrame` (dual-pane `ttk.PanedWindow`: Suppliers | Purchase Orders; left `ttk.Treeview` ID/Name/Contact/Phone + Add/Edit/Delete/Set-Preferred; right `ttk.Treeview` PO ID/Supplier/Date/Status/Total/Expected + status-filter `CTkOptionMenu` + New PO/Auto-Reorder/Edit PO-Items/Receive Order; `grid_propagate(False)` tree containers; observer-driven auto-refresh; Receive enabled only for `Submitted` POs; `_debug_layout_geometry()` zero-dim/clipping probe). `setup_supplier_order_tab(self, parent=None)` packs the frame into the TabViewCompat tab content frame (mirrors `setup_inventory_management_tab`). State machine extended with `Cancelled` (Draft/Submitted→Cancelled). 14 new i18n keys (`all`,`id`,`date`,`po_id`,`supplier`,`purchase_orders`,`cancelled`,`delete_supplier`,`set_preferred`,`new_po`,`auto_reorder`,`edit_po_items`,`po_status_draft`,`po_status_received`) added to `en.json` + propagated to all 6 locales. main_app wiring snippet delivered (user-insert): nav-icon 🚛, `self.tab_supplier_order = self.tab_view.add(i18n.t("supplier_order_title"))`, `setup_supplier_order_tab(self)`, on_tab_change refresh. Verified: py_compile OK; headless CTK smoke FRAME_SMOKE_OK (observer populate, status filter, state-gated Receive, auto_reorder, duplicate guard, delete); phase9 24/24, phase16 25/25, exhaustive 105/105. **COMPLETE: M92**
- [COMPLETE] Phase 17: Legacy Checkout UI Overhaul & Stub Elimination — Eliminated all `pass` stubs from `ui_checkout_tab.py` (`_print_receipt`, `_checkout_add_item`, `_on_checkout_product_change`), 2 text-dump `messagebox.showinfo` stubs (`_pos_show_receipt_detail`), and the `InsurancePanel._edit()` except-block swallower. Implemented `ProductPickerDialog` and `ReceiptDetailDialog` modal classes in `ui_pos_panels.py` with AsyncUI thread-safe loading. Added product combobox + "Add Item" button to checkout tab. Wired 8 of 9 Status Dashboard TaskPanel buttons to existing tabs; `task_iv_orders` shows contextual guidance. Added 5 methods to `PharmacyApp` (`_new_prescription`, `_open_database`, `_save_all`, `_open_preferences`, `_show_about`) for EnterpriseMenuBar commands; updated menu bar fallbacks from silent `None` to `messagebox.showinfo`. Implemented supplier lookup (`_on_lookup`) via `ndc_dictionary` barcode/ndc/name lookup chain. Fixed duplicate `checkout_items_count_label`. Added 22 new i18n keys to all 6 locale files. 28 new tests in `test_phase17.py` — all PASS; existing 25 Phase 16 tests still PASS. Locked files (`rx_db.py`, `rx_config.py`, `rx_strategies.py`) unmodified. **COMPLETE: M93**
- [COMPLETE] Phase: RBAC — Dynamic Role-Based Access Control — `archive/auth_crypto.py` (scrypt salted hashing + `hmac.compare_digest` constant-time verify); `archive/database.py` + `archive/db.py` backend parity: RBAC DDL (`roles`/`users`/`permissions`/`role_permissions`/`system_settings`) + 13 feature keys + 4 seed roles (owner/manager/pharmacist/cashier) + DAL (`get_roles`,`get_permissions`,`get_role_permissions`,`get_user_role_id`,`get_role_name`,`get_user_permissions`,`create_role`,`assign_role_to_user`,`set_role_permissions`,`grant_permission`,`toggle_permission`,`create_user`,`authenticate_user` w/ 5-fail lockout,`verify_user_pin`,`set_owner_override_password`,`verify_owner_override`,`count_users`); `archive/auth_session.py` (in-memory session + PIN quick-auth TTL + pluggable Owner-override hook); `archive/authz.py` (`check_permission`,`access_denied`,`require_permission` decorator,`SENSITIVE_FEATURES`,`create_first_owner` anti-escalation guard); `archive/ui_auth.py` (`CreateOwnerAccountDialog` Option A, `LoginDialog`, `PinPrompt`, `OwnerOverridePrompt`); `archive/ui_admin_roles.py` (Owner permission matrix + change override, audit-logged); `archive/ui_enterprise_navigation.py` Tools menu "Roles & Permissions"; `archive/main_app.py` `_wire_rbac()` login gate + first-run Owner creation + Ctrl+Shift+A. `archive/test_rbac.py` (6 tests PASS) + headless UI smoke. Bootstrap Owner override password = `ChangeMe!Owner` (change on first run via Admin panel). **COMPLETE**
 - [COMPLETE] Phase 17.5: Localization, Currency, Tax & Region Banner (2026-08-08):
   - **`localization_manager.py` (NEW):** singleton `LocalizationManager` — single source of truth for region (`US`/`GB`/`DE`), currency symbol/code/position/decimal separators, tax term, date format, drug-code label. `detect_region()` chain: saved override to OS locale (`GetUserDefaultLocaleName`) to IP geolocation (2s timeout, cached 24h, opt-out) to `US`. `format_money`/`parse_money` produce `$1,234.50`/`£1,234.50`/`1.234,50 €`. `get_field_visibility()` returns region-gated fields.
   - **`currency.py` (NEW):** `CurrencyFormatter` + `fmt`/`parse`/`symbol`/`tax_term`/`fmt_date` module-level helpers. Lazy-imports LocalizationManager; degrades to USD.
   - **`database.py`:** `set_kv`/`get_kv`/`load_config` helpers (idempotent INSERT OR REPLACE).
   - **`rx_config.py` (T8 fix):** `get_region()`/`set_region()` adapter-delegate to LocalizationManager (writes `region` key not `rx_region`; `UK`->`GB`).
   - **Currency call sites (T10):** 106 `f"${x:.2f}"` replaced with `self.app.currency.fmt(x)` across 13 files; parse sites `.replace("$","")` to `self.app.currency.parse()`. 6 locale keys stripped of `$` in all 6 locale files.
   - **Nav indicator (G2.3):** persistent region/currency label on `NavigationDrawer` row 98 — survives dashboard refresh.
   - **Interactive banner (G3.1/G3.2):** `ui_region_banner.py` — dismissible, RBAC-aware, audit-logged; child of `NavigationDrawer`.
   - **Enterprise Settings (G4.1/G4.2/T8):** region selector command wrapped with `require_permission("settings.manage")`; audit-log on change; `region_selector.set()` uses `cm.get_region()`.
   - **Verification:** 32/32 `test_rx_config` PASS, 23/24 `test_phase9` PASS (1 pre-existing `async_ui` mock failure), integration smoke OK.
