# Frozen runtime dependencies for the edge kiosk backend (Phase 4).
# Install into the bundled Python venv during Inno setup (see setup.iss / install.ps1).
# Versions mirror pyproject.toml constraints; pin exact versions before shipping.
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
sqlalchemy>=2.0,<3.0
aiosqlite>=0.20,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
bcrypt>=4.2,<5.0
pyjwt>=2.9,<3.0
python-dotenv>=1.0,<2.0
structlog>=24.0,<25.0
slowapi>=0.1.9,<1.0
limits>=5.0,<7.0
